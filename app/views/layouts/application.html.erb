<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
    <nav>
      <h1>Bar Cart Assistant: Cocktail recommendations based on your bar
      </h1>
      <ul>
          <li><a href="/materials">Materials</a></li>
        <% if session.fetch(:user_id) != nil %>
          <li><a href="/purchases">My Bar</a></li>
          <li><a href="/user_sign_out">Sign out</a></li>
        <% else %>
          <li><a href="/user_sign_up">Sign up</a></li>
          <li><a href="/user_sign_in">Sign in</a></li>
        <% end %>
      </ul>
    </nav>

    <% if notice.present? %>
      <div style="border: thin green border; color: green">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert.present? %>
      <div style="border: thin red border; color: green">
        <%= alert %>
      </div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
