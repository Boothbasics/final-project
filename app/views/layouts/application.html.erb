<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">

  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <h4>Bar Cart Assistant</h4>

      <div class="container-fluid">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Recommendations</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/purchases">My Bar Cart</a>
          </li>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/materials">Cocktail Materials</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/recipes">All Recipes</a>
          </li>
          <% if session.fetch(:user_id) != nil %>
            <li><a class="nav-link" href="/user_sign_out">Sign out</a></li>
          <% else %>
            <li><a class="nav-link" href="/user_sign_up">Sign up</a></li>
            <li><a class="nav-link" href="/user_sign_in">Sign in</a></li>
          <% end %>
        </ul>
      </div>

    </nav>

    <% if notice.present? %>
      <div style="border: thin green border; color: green">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert.present? %>
      <div style="border: thin red border; color: green">
        <%= alert %>
      </div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
